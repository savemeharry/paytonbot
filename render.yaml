services:
     - type: web
       name: telegram-bot
       runtime: python
       buildCommand: pip install -r requirements.txt
       startCommand: python webhook.py
       envVars:
         - key: PYTHONUNBUFFERED
           value: true
